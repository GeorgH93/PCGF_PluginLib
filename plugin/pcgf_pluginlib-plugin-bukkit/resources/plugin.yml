name: "${pluginName}"
author: "${author}"
website: "${project.url}"
description: "${description}"
main: "at.pcgamingfreaks.PluginLib.Bukkit.PluginLib"
version: "${project.version}-T${timestamp}"
api-version: "1.13"
softdepend: [ "PlaceholderAPI" ]
folia-supported: true